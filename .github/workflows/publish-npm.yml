name: publish-npm
on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 📦 INSTALL NPM LTS
        uses: actions/setup-node@v3
        with:
          node-version: lts/*
      - name: 🚛 INSTALL DEPENDENCIES
        run: ls -la